
#ifndef PMERGEME_HPP
#define PMERGEME_HPP

#include <iostream>
#include <vector>
#include <deque>

int isNumber(std::string str);
void parse(std::vector<int>& vec, std::deque<int>& dq, int ac, char **av);
#endif
